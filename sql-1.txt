Create table hospital(hid int,hname varchar(20),haddress varchar(20));
git start used cmd: git init

git commit cmd :git commit -m "Complete filename"


git check status: git stauts.

git check log :git log

add file into git : git add .

remove file git :git rm filename


 after modify git file check :git diff filename.


rollback file :git chekout filename


 push an existing repository from the command line

git remote add origin https://github.com/wmayu2020/personalsite.git
git branch -M master
git push -u origin master



to remove all file into git :git  rm --cached -r .


again add all file into git by using gitignore file:  put txt :: .secrete.txt


and :git add .

after check status

Again Commit all files
git commit -m "Inital commit"

